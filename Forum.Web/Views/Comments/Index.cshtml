@model Forum.Web.ViewModels.CommentsIndexVm
@{
    ViewData["Title"] = "Index";
}

<h2>@Model.Post.Title</h2>

<ul class="list-group">
    @foreach (var com in Model.Comments)
    {
        <li class="list-group-item">@com.Content</li>
    }
</ul>

